import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("السبحة")
                .font(.largeTitle)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
